services:
  server:
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/firebase-service-account.json
    volumes:
      - type: bind
        source: ${FIREBASE_CREDENTIALS_FILE:?Set FIREBASE_CREDENTIALS_FILE to a service-account JSON outside the repository}
        target: /run/secrets/firebase-service-account.json
        read_only: true
